const { SHELL, HOMEBREW_PREFIX, npm_lifecycle_script } = process.env

const hero = ["flash", "superMan", "wolverine", "batman"]

const [ , , , batman] = hero
// console.log(batman )
// console.table({SHELL, HOMEBREW_PREFIX, npm_lifecycle_script})
